package unidade3;

import javax.swing.JOptionPane;

public class AvaliacaoNota {

	public static void main(String[] args) {
		
		int nota = Integer.parseInt(JOptionPane.showInputDialog(null, "Digite a nota"));
		
		if ((nota >=0) && (nota <=100)) {
			JOptionPane.showInputDialog(null, "A nota é " + nota);
			
		}else if (nota >=90) {
			JOptionPane.showInputDialog(null, "Conceito Exelente");
		}else if (nota >=70 && nota <=89) {
			JOptionPane.showInputDialog(null, "Conceito Bom");
		}else if (nota >=50 && nota <=30) {
			JOptionPane.showInputDialog(null, "Conceito Regular");
		}else{
			JOptionPane.showInputDialog(null, "Conceito Insuficiente");
		}

	}

}
